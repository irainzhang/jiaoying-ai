> 本文为 V2.2 网关历史说明。V3 本地服务未启用此网关，当前接入约定请看 [V3接口与演练说明.md](V3接口与演练说明.md)。

# AI 协同助手交流入口

已新增侧栏第一项“AI 协同助手”和业务页右下角“问问 AI 助手”。可输入文字、连续交流、使用示例问题、保留跨页对话，并查看当前演练上下文。

## 当前可直接体验

没有配置模型时，明确显示“本地导览 · 未连接大模型”。本地导览按有限规则回答当前情况、未安排对象、车辆路线和封桥情景；不能作为大模型效果展示。

- 问“总结当前情况”：依据当前输入、联系人和核验记录回答。
- 问“谁还没安排”：引用当前有效方案；旧方案失效时不会继续称旧安排有效。
- 问“如果东桥封闭会怎样”：先提供建议；点击“按东桥封闭重算草案”才更改本地演练道路并运行算法。
- 输入“南湾户需要轮椅接送”：点击“带入回执核实”后，原文进入现有反馈页，人工修正并登记。不自动改变人数或确认转移。
- 没有明确家庭时，先选择对象；涉及多户时应分别核实。
- 开始执行后仍遵守原状态约束，不能从对话绕过冻结或解除待协调事项。

每条回答记录所用输入版本和操作记录序号；演练变化后旧操作卡失效。正在等待模型时可以切换页面；若期间重置演练，迟到回答丢弃。

## 后续连接 DeepSeek

按团队选择为 DeepSeek 预留后端接口。目前未填写密钥，也未调用真实模型；后续准备好账号后再配置即可。采用 Chat Completions 格式，参考[DeepSeek 官方请求文档](https://api-docs.deepseek.com/api/create-chat-completion/)。其他服务需要支持同样的 `POST /chat/completions`、`messages` 和文本 `choices[0].message.content` 格式，实际兼容性需用对应服务验证。

1. 在本目录将 `.env.example` 复制为 `.env`。
2. 在本机填写三个值：

```dotenv
JIAOYING_AI_BASE_URL=https://api.deepseek.com
JIAOYING_AI_MODEL=账号可用的模型名称
JIAOYING_AI_KEY=在本机填写访问密钥
```

基础地址不要带 `/chat/completions`；程序会追加该路径。远程服务使用 HTTPS，本机兼容服务可用 localhost HTTP。DeepSeek 基础地址见[官方快速开始](https://api-docs.deepseek.com/)。模型名称填写账号当时可用的名称，计费与可用模型以官方服务为准。

3. 重新启动本地服务 `node server.mjs`，访问 `http://127.0.0.1:8765/#assistant`，点击“刷新状态”。
4. 页面会先显示“模型已配置，待首次请求”；成功收到回复后才显示“模型已响应”。未配置、配置错误、服务拒绝或超时均显示真实状态，不冒充模型回答。

密钥仅由本机后端读取，不进入浏览器脚本、网页响应或对话导出；不要把 `.env` 分享到演示包或源码库。配置状态检查不请求模型，发送提问时才调用配置服务。此时问题、最近部分对话及当前虚构演练上下文会发送到该服务。

本版接口已用本机测试替身验证请求格式、服务错误和配置隔离；未使用真实模型账号做端到端验证，因此不宣称已接通某个具体服务。模型回答是建议文本；操作卡仍由本地有限规则生成，不是模型自主选工具。公开预案检索、模型结构化抽取与自主工具编排尚未实现。

## 离线与本地服务

双击原 `启动演示.cmd` 仍可离线使用本地导览。调用真实模型必须通过 `server.mjs` 提供的本地页面，直接打开 HTML 文件不会连接模型。

对话默认保存在当前页面内存，刷新清空；“导出演练”会包含对话文本、来源模式和版本标记。只清空对话不会重置业务演练，重置演练会同时清空对话。

## GitHub Pages 在线演示

在线静态演示提供全部算法和本地规则导览，页面不会在 GitHub Pages 请求模型后端。要使用 DeepSeek，请下载项目并通过本机 `server.mjs` 启动后配置；浏览器中不填写密钥，GitHub Pages 也不托管真实 `.env`。
