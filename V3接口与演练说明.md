> 历史版本说明。当前 V3.5 的能力、入口和限制见 [README.md](README.md)。

# V3 接口与演练说明

2026-09-22。本轮依据会议核对稿及用户补充实施，仅本机运行。

V3.1 更新：AI 对话已移至首屏。默认展示瑞安市城区和飞云江两岸真实 OSM 矢量底图（1,461 要素，本地文件、WGS84），可拖动/缩放；原合成路网作为独立调度视图。地图来源见 `dist/assets/maps/SOURCES.md`，真实道路尚未接入调度求解。

## 已确认取舍

- 指挥端、反馈端都是网页；本机共享服务已实现同步。跨设备联网、登录权限、数据库持久化暂不在本版。
- 使用“浙江省瑞安市”行政名称。默认选瑞安市城区及飞云江两岸真实地理片区；尚未选定实际转移对象和安置点，合成调度拓扑不得当作导航。
- Agent 和彩云接口先准备、后接入；保留 DeepSeek 为后续候选服务。没有访问任何模型/天气接口。
- 系统口播使用语音合成；语音输入继续使用浏览器识别。播报与识别互斥，不保存录音。浏览器识别或部分系统音色自身可能需要联网。
- 本轮不添加单点积水深度预测。模拟天气变化只触发复核，不推断封路、伤亡或受影响人数。
- 不更新 GitHub；现有公网版仍为 V2.2。没有购买、租用或配置公网服务。

## 数据流与人工边界

现场网页提交 → 共享服务记为待核实 → 指挥端核实 → 更新已确认路况/新增需求 → 本地计算及约束校验 → 候选草案与基线/备选 → 人工确认 → 模拟发布 → 按站点登记 → 到达后人工核验。

自由文本“已到达”、模型回答和车辆位置都不直接确认安全。医疗、其他险情先入协调事项，没有虚构救护车或医疗调度能力。道路多条反馈逐条处理，全部已采纳障碍排除后才恢复通行。新增人员单独建组，不修改原车内既有人数。

已有发布方案与新草案同时保留，旧路线含已确认封路时禁止继续推进；未受影响车辆可以推进，但会使待确认草案过期。车辆从最后登记节点与分钟继续，不是连续 GPS 或实时仿真。每辆车单趟，既有家庭与必须同行组不拆分；村级独立人员按规则成组。最多 500 名有效人员、200 个有效组。超过 8 个待接组用有界启发式，仍校验同一容量与执行约束。

同级天气变化只刷新信息；跨演练等级、核实封路、新增人员、准备阶段资源编辑触发复核。未实现时间冷却、收益阈值或通用语义事件自动去重（V3.4 村级新增对同村/同集合点/相同人数的 30 分钟重复上报提供确认提示）；用输入/执行版本和人工发布防止无意替换。Plan B 是当前同一快照下的可行备选；发生新封路等变化后仍须重新计算，不承诺抵御所有未知情景。

## V3.3 现场环节与语音助手

已接入浏览器 SpeechRecognition/webkitSpeechRecognition。转写服务是否可用由浏览器环境决定；本地 `field-assistant.js` 只进行保守规则整理，无大模型调用。语音结束只生成待确认草稿，不自动发请求。

现场快捷环节调用 `field-progress`，载荷包含 `stage: ack/contact/board/arrive`、`vehicleId`、当前 `planId`、联系/上车时的 `householdId`、`reporter`、`source: quick/voice/text/manual`、原话 `text`。接收方案与联系家庭独立；到达只设为 arrived，指挥端仍需 verify。道路、人员、医疗、险情走原 report/review 流程。

`data.fieldEvents` 保存最新 100 条现场确认事件；`data.taskAcks[vehicleId]` 记录当前已接收方案。草稿绑定服务 session/revision，任何输入变化或版本过期都不能直接提交。编辑中的原话仅留在该网页，确认后才进入共享事件。

## 本机共享 API

`GET /api/v3/state` 返回 `{session,data,metrics,villageLedger,blockedVehicles,integrations,transport,capabilities}`。

`GET /api/v3/events` 为 SSE，事件名 `state`，内容为完整快照，事件 ID 为 `session:revision`。首次连接/重连即推送当前状态；成功操作广播，失败及重复请求不重复广播。15 秒心跳，最多 24 个连接，有背压清理和轮询后备。

`GET /api/v3/state?after=<revision>&session=<session>` 在没有更新时返回 `unchanged:true`。

`POST /api/v3/action` 使用 JSON：

```json
{
  "action": "report",
  "payload": {
    "kind": "road",
    "location": "east",
    "text": "演练东桥倒树，车辆无法通行",
    "reporter": "现场演示员"
  },
  "expectedRevision": 2,
  "session": "读取状态接口返回的会话编号",
  "requestId": "为本次提交创建唯一编号"
}
```

动作：`generate`、`weather`、`report`、`review`、`resolve`、`confirm`、`contact`、`start`、`step`、`verify`、`edit`、`reset`。具体字段见 `exercise.cjs` 和 `tests/v3-exercise.test.cjs`。网页提交不是给真实人员发送消息。

每次变更带 `expectedRevision` 与 `session`。409 表示另一网页或服务重启使依据变化，该操作未执行；须读取最新状态重新核对。422 为业务校验失败，事务回滚。相同 requestId 和内容重试返回当前状态，不重复新增；同编号不同内容返回 409。最多保留 500 个去重记录，内存状态与去重记录不跨服务重启。

JSON 请求上限 16KB。服务仅绑定回环，检查 Host、Origin 与 Content-Type。这些是本机演示边界，不是生产账号鉴权方案。浏览器刷新不会丢掉服务数据，重启会丢掉，可通过启动环境变量 JIAOYING_STATE_FILE 恢复导出 JSON，尚无网页导入或自动持久化。

## Agent 预留

`GET /api/v3/integrations` 可查看状态与契约。`/api/v3/agent` 固定返回 501 和契约，表示尚未连接，不返回伪造的模型答案。

契约 `jiaoying-agent-v1`：输入 requestId、utterance、snapshot、inputVersion、executionVersion；输出 requestId、summary、proposedActions、evidence 及相同版本。接入实现还应带 session 和 revision，用于最终网页操作校验。

预留工具：`read_state`（读快照）、`calculate_draft`（生成候选，不发布）、`prepare_report`（读报告）、`propose_scenario`（提出情景建议）。这些目前是契约名称，尚未实现网络 Agent 调度器。不要把确认、发布、安全核验暴露为 Agent 可自行执行的操作。

后续由 Agent 服务调用本地计算器，返回工具结果和数据依据；前端只接受当前快照的建议，业务动作仍交由服务器再校验。需要补充 Agent URL、鉴权方式、流式/非流式协议、失败格式、工具调用适配、超时取消和公开预案检索。密钥只放后端，不进前端或 GitHub。

## 彩云天气预留

`integrations.mjs` 提供查询模板 `prepareCaiyunRequest` 和纯数据转换 `normalizeCaiyun`，没有 fetch、没有读取凭证，也不会自动激活接口。

准备配置：示范点真实经度/纬度、坐标系、覆盖范围、数据更新时间、后端 App Key/Secret 和签名方式。当前经纬度为空；没有用猜测坐标冒充瑞安真实天气。

- URL 顺序为 longitude,latitude；需显式 `unit=metric:v2`。
- 实时 `precipitation.local.intensity` 是当前降水强度 mm/h，不能直接填入当前演练“最近一小时累计 mm”栏。真实接入应另建强度/预报展示，累计量采用明确的观测序列来源。
- 小时 precipitation 数组为对应预测时刻的强度；保持时间戳，不能说是已发生降水。
- 检查总 status 和各数据块 status；缺失值为 null，不替换为 0。当前转换器对服务时间超过 20 分钟、缺失或异常未来时间标记 stale，此为演示策略，不是彩云服务承诺。
- 查询模板用后端凭证占位符；正式客户端需依官方推荐完成 HMAC 签名鉴权、超时、HTTP/JSON 双层错误校验、缓存及限流。目前尚未实现网络请求与签名器。
- 真实请求失败应展示不可用和最后更新时间，不能静默切回模拟数据再称实时。

官方参考（2026-09-22 核对）：[天气综合接口](https://docs.caiyunapp.com/weather-api/v2/v2.6/6-weather.html)、[实时天气](https://docs.caiyunapp.com/weather-api/v2/v2.6/1-realtime.html)、[小时预报](https://docs.caiyunapp.com/weather-api/v2/v2.6/3-hourly.html)、[单位](https://docs.caiyunapp.com/weather-api/v2/v2.6/tables/unit.html)、[鉴权](https://docs.caiyunapp.com/weather-api/v2/v2.6/auth.html)。

## 数据依据与比赛表达

当前没有接入公开防汛预案知识库，不展示虚构预案条款。真实底图已接入 OpenStreetMap 历史矢量数据，但不提供实时路况或灾情。地区名称参考[国务院公报中的撤县设市记录](https://www.gov.cn/gongbao/shuju/1987/gwyb198708.pdf)。区域背景不能当作真实路网或灾情数据来源。

原“叫应”材料仍只有公开报道，没有原系统代码、接口或合作授权。本 Demo 为独立原型，路径导航不能当作对原产品的首次发明；本轮重点展示约束下的车辆与安置联合安排、反馈核实、途中剩余任务重规划及可审核闭环。

模拟基线按风险优先与近点分配构造，有相同硬约束。没有实际部门记录，不称“优于真实人工派车”。合成结果不证明真实减灾效果。赛事数据资格、真实对照实验、最终提交材料和截止日期仍需团队对照官方材料确认。

## 下一轮最有价值的材料

1. 一块可公开展示的瑞安具体片区，带来源许可的道路、公开安置点及容量、公开预案条款。
2. 业务人员确认的调度规则：车型、轮椅位、出发点、接人用时、分组方式、转运多趟和协调权限。
3. Agent 的接口说明与测试地址；彩云后端账号配置及核实后的点位。无需把密钥发在聊天中。
4. 两台设备演示的网络范围与访问方式；届时再决定局域网/部署、身份权限和持久化。
5. 是否需要水深模型及可验证数据；未确认前维持本版风险与事件演练范围。


默认本机端口为 8767。`JIAOYING_STATE_FILE` 可在启动时读取已有完整快照或 data 对象进行恢复；不属于数据库持久化，服务不会自动保存后续更改。升级启动器读取 tmp/before-v34-state.json；正常停止前仍应导出最新记录。

## V3.4 村级接口

`capabilities.villageReporting=true`、`capabilities.version="3.4"`。`data.villages` 为演示目录，`data.villageReports` 为批次历史，`villageLedger` 为服务端汇总。

- `village-report`：villageId、mode（increment/snapshot/correction）、people、pickupId（空表示未知）、assistancePeople / wheelchairPeople（未知为 null）、groupPolicy（unknown/splittable/together）、text、reporter、source、duplicateAcknowledged。更正还需 targetId；总数观测需 observedAt 和 scope="waiting"。
- `village-review`：id、decision（accept/reject）、note，可同时补 pickupId、assistancePeople、wheelchairPeople、groupPolicy。确认卡和审核框里的总人数不能静默不一致，审核框人数只读；要改人数走更正批次。
- `village-complete`：id、pickupId、assistancePeople、wheelchairPeople、groupPolicy、note；只补齐已采纳且仍待补信息的批次。

审核/补齐触发新草案，不自动发布。原批次的分组通过 sourceBatchId 关联；更正后的原组 stage=superseded 留作历史，排除有效人数、地图计数与求解。原发布路线若含被更正组则暂停推进，等待重新规划与确认。重复请求继续使用 session/revision/requestId 协议；疑似同批重复不是自动丢弃，用户必须明确声明另一批。

村级 Agent 将来可替换 `village-assistant.js` 的 prepare() 输出，仍应返回待确认 proposal，由当前服务器动作校验。现阶段不访问模型 API。
