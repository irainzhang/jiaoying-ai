# V3.4 村级上报演示说明

本次升级在本地完成，未发布到 GitHub。DeepSeek 与彩云未接入，语音使用浏览器识别；识别后的字段由本地规则整理。示范村庄为演示村 A/B/C，集合点连接合成路网，没有虚构真实村庄坐标。

## 打开

双击 `启动V3.4演示.cmd`，打开 8767 端口的指挥与现场网页。启动器检查后端村级功能，若该端口被其他版本占用，会提示而不杀掉旧服务。原 8765 服务未重置，已保存升级前 JSON 快照 `tmp/before-v34-state.json`；新版启动时可从此快照恢复。

快照保留原有执行进度。如果原演练车辆已经到达，本趟车辆不会自动变成可用车辆，新上报会留在待协调队列。若需要从零演示完整接送流程，请先导出需要保留的记录，再在新版“接口与范围”中手动重置本场演练。

刷新网页保留服务内状态；服务停止后，后续操作不会自动保存。升级快照不是持续更新的数据库，应使用“导出完整演练 JSON”保存最新进度。两个网页须连接同一个本地端口，当前不支持不同设备访问。

## 建议演示过程

1. 现场网页默认显示“村级人数上报”，选择“演示村 A”和“村委会集合点（演示）”。
2. 说出或输入：**新增20人，其中3人需要协助，包含1名轮椅人员。都是独立人员，可以分组。**
3. 点击整理后，确认卡显示总数 20、协助 3、轮椅 1。可以修改字段，最后点击“确认上报新增20人”一次完成提交。
4. 指挥台的村级待办、现场实况即时出现这条记录。“待核实新增”为 20，“已核实待接”此时不增加。
5. 指挥台核实并填写依据，接受后生成接送组与新调度草案。草案不会自动发布；有限车辆的单趟容量不足时明确列出待协调人员。
6. 现场端选择“更正这批人数”，改为 18 人。审核前原批次继续计数，审核后只减少 2 人，原批次归档留痕。
7. 换到演示村 B，只说“新增4人，集合点和协助需求还不清楚”。可先提交并核实；4 人计入待接与待协调，但不生成路线。指挥台补齐集合点、协助/轮椅人数及同行关系后，才形成接送组。
8. 再演示“当前待转移总数”：填写清点时间，说“目前还有10人等待转移”。核实后只记录此时观测，不与已有批次相加。
9. “任务与异常反馈”保留原接单、联系、上车、到达及路障反馈流程。接送组仍必须联系、按当前发布方案执行，到达后由指挥台人工核验。

## 人数与分组口径

| 字段或状态 | 含义 |
| --- | --- |
| 待核实新增 | 未审核的新增批次，尚不计入有效转移总数 |
| 已核实待接 | 当前仍等待接送的有效人员，包含待补信息人数 |
| 待补信息 | 集合点、协助人数、轮椅人数或同行关系尚未齐全，先保留需求 |
| 当前总数观测 | 指定清点时刻的本村待转移总量，仅作对账参考；不含已上车、已到达或已核验人员 |
| 更正批次 | 替换同一批次的有效人数，不是再新增一批；已有人员上车的批次不允许此种更正 |
| 协助与轮椅人数 | 轮椅人数是协助人数的子集，协助人数是总人数的子集，禁止重复相加 |
| 独立人员 | 人工确认可分组后，形成最多 4 人的接送组；轮椅位单独核对 |
| 整组同行 | 作为不可拆分的组；无满足容量或轮椅位的车就保留待协调 |

同村、同集合点（包括未知集合点）、相同新增人数在 30 分钟内重复出现，会提示疑似重复。确认“是另一批”后允许上报，不自动删除或自动合并；相同网络请求编号重试则只生效一次。

## 验证与实际边界

已用两个真实浏览器网页验证：增报、核实回传、批次更正、先报后补、独立人员与整组同行、未知点位疑似重复提示、总量观测不累加。114 项相关行为测试通过，覆盖人数守恒、车载锁定、轮椅/车辆/安置容量、封闭道路、旧路线暂停、SSE 同步、幂等、版本冲突和草稿保留。

语音转写与上述文字输入使用同一整理和确认流程；本轮未进行真实麦克风口述。浏览器语音服务可能联网，识别不可用时可直接输入文字或手动填写确认卡。

本版支持最多 500 名有效人员 / 200 个有效接送组。超过 8 个待接组采用有界启发式搜索，不保证全局最优；所有方案通过容量、执行状态与人数校验。真实地图仍仅供地域展示，调度依然基于合成路网，不能作为现实避险导航。

## 后续接入所需资料

- 真实乡镇和村庄目录、可用集合点及经过核实的坐标。
- 各村人数统计口径、同行家庭识别方式、联络与核实责任流程。
- 车辆座位和轮椅位、可用状态、安置容量，以及真实道路状态来源。
- DeepSeek Agent 接口与天气服务由后端配置，前端不保存密钥。Agent 返回待确认字段建议，继续使用现有服务器审核与调度约束。
